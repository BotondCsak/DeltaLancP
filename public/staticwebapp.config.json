{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/plasmic-host", "/irm"]
  },
  "routes": [
    {
      "route": "/irm",
      "allowedRoles": ["anonymous", "authenticated"]
    }
  ]
}
